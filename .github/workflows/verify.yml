name: Verify

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  verify-macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build and test on macOS + iOS Simulator
        run: bash scripts/verify-on-mac.sh
